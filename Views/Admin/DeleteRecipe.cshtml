@model YemekTarifleri.DTOs.RecipeDto

@{
    ViewData["Title"] = "Tarifi Sil";
}

<div class="max-w-2xl mx-auto">
    <!-- Başlık -->
    <div class="mb-6">
        <h1 class="text-3xl font-bold text-gray-900">🗑️ Tarifi Sil</h1>
        <p class="text-gray-600 mt-1">Bu işlem geri alınamaz. Emin misiniz?</p>
    </div>

    <!-- Uyarı Kutusu -->
    <div class="bg-red-50 border-2 border-red-300 rounded-lg p-6 mb-6">
        <div class="flex gap-4">
            <div class="text-3xl">⚠️</div>
            <div>
                <h2 class="text-lg font-bold text-red-900 mb-2">Dikkat: Geri Alınamaz İşlem</h2>
                <p class="text-red-800">
                    <strong>"@Model.Ad"</strong> tarifini silmek üzeresiniz. Bu işlem geri alınamaz ve tarif veritabanından tamamen kaldırılacaktır.
                </p>
            </div>
        </div>
    </div>

    <!-- Tarif Özeti -->
    <div class="bg-white rounded-lg shadow-md p-6 mb-6">
        <h3 class="text-lg font-semibold text-gray-900 mb-4">Silinecek Tarif Bilgileri</h3>
        
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <!-- Sol Taraf -->
            <div>
                @if (!string.IsNullOrEmpty(Model.ImagePath))
                {
                    <img src="@Model.ImagePath" alt="@Model.Ad" class="w-full h-48 object-cover rounded-lg mb-4" />
                }
                else
                {
                    <div class="w-full h-48 bg-gray-300 rounded-lg flex items-center justify-center mb-4 text-gray-500 text-4xl">
                        📷
                    </div>
                }

                <div class="space-y-3">
                    <div>
                        <p class="text-sm font-medium text-gray-600">Tarif Adı</p>
                        <p class="text-lg font-semibold text-gray-900">@Model.Ad</p>
                    </div>
                    <div>
                        <p class="text-sm font-medium text-gray-600">Kategori</p>
                        <p class="text-gray-900">@(Model.Kategori ?? "Belirtilmemiş")</p>
                    </div>
                </div>
            </div>

            <!-- Sağ Taraf -->
            <div class="space-y-4">
                @if (!string.IsNullOrEmpty(Model.Aciklama))
                {
                    <div>
                        <p class="text-sm font-medium text-gray-600 mb-1">Açıklama</p>
                        <p class="text-gray-700">@Model.Aciklama</p>
                    </div>
                }

                <div class="grid grid-cols-2 gap-4">
                    <div class="bg-blue-50 rounded-lg p-3">
                        <p class="text-sm font-medium text-blue-600">Hazırlama Süresi</p>
                        <p class="text-2xl font-bold text-blue-900">
                            @(Model.HazirlamaSuresi.HasValue ? $"{Model.HazirlamaSuresi}'" : "—")
                        </p>
                    </div>
                    <div class="bg-purple-50 rounded-lg p-3">
                        <p class="text-sm font-medium text-purple-600">Zorluk Seviyesi</p>
                        <p class="text-2xl font-bold text-purple-900">
                            @switch(Model.ZorlukSeviyesi)
                            {
                                case "Kolay":
                                    <span>🟢</span>
                                    break;
                                case "Orta":
                                    <span>🟡</span>
                                    break;
                                case "Zor":
                                    <span>🔴</span>
                                    break;
                                default:
                                    <span>—</span>
                                    break;
                            }
                            @Model.ZorlukSeviyesi
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Malzemeler Önizlemesi -->
    @if (!string.IsNullOrEmpty(Model.Malzemeler))
    {
        <div class="bg-white rounded-lg shadow-md p-6 mb-6">
            <h3 class="text-lg font-semibold text-gray-900 mb-3">📋 Malzemeler</h3>
            <div class="bg-gray-50 rounded p-4 max-h-40 overflow-y-auto">
                <p class="text-gray-700 text-sm whitespace-pre-wrap">@Model.Malzemeler</p>
            </div>
        </div>
    }

    <!-- Yapılış Önizlemesi -->
    @if (!string.IsNullOrEmpty(Model.Yapilis))
    {
        <div class="bg-white rounded-lg shadow-md p-6 mb-6">
            <h3 class="text-lg font-semibold text-gray-900 mb-3">👨‍🍳 Yapılış</h3>
            <div class="bg-gray-50 rounded p-4 max-h-40 overflow-y-auto">
                <p class="text-gray-700 text-sm whitespace-pre-wrap">@Model.Yapilis</p>
            </div>
        </div>
    }

    <!-- Onay Butonları -->
    <div class="flex gap-4">
        <form method="post" asp-action="DeleteRecipe" asp-route-id="@Model.Id" class="flex-1">
            <button type="submit" class="w-full bg-red-600 hover:bg-red-700 text-white px-6 py-3 rounded-lg font-semibold transition-colors">
                ✅ Evet, Sil
            </button>
        </form>
        <a asp-action="Recipes" class="flex-1 bg-gray-400 hover:bg-gray-500 text-white px-6 py-3 rounded-lg font-semibold transition-colors text-center">
            ❌ Hayır, İptal Et
        </a>
    </div>
</div>
