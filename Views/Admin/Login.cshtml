@{
    ViewData["Title"] = "Admin Giriş";
}

<div class="min-h-screen flex items-center justify-center bg-gradient-to-br from-emerald-50 to-teal-50 px-4">
    <div class="w-full max-w-md">
        <!-- Başlık -->
        <div class="text-center mb-8">
            <div class="text-5xl mb-4">🔐</div>
            <h1 class="text-3xl font-bold text-gray-900">Admin Paneli</h1>
            <p class="text-gray-600 mt-2">Lütfen şifrenizi giriniz</p>
        </div>

        <!-- Form -->
        <form method="post" class="bg-white rounded-lg shadow-lg p-8 space-y-6">
            @Html.AntiForgeryToken()

            <!-- Hata Mesajı -->
            @if (!string.IsNullOrEmpty(ViewBag.Error))
            {
                <div class="bg-red-50 border-l-4 border-red-500 p-4 rounded">
                    <div class="flex">
                        <div class="text-red-500 text-xl mr-3">⚠️</div>
                        <div>
                            <p class="text-red-800 font-semibold">Hata</p>
                            <p class="text-red-700 text-sm">@ViewBag.Error</p>
                        </div>
                    </div>
                </div>
            }

            <!-- Şifre Alanı -->
            <div>
                <label for="password" class="block text-sm font-semibold text-gray-700 mb-2">
                    Admin Şifresi
                </label>
                <div class="relative">
                    <input
                        type="password"
                        id="password"
                        name="password"
                        placeholder="Şifrenizi giriniz..."
                        required
                        autofocus
                        class="w-full px-4 py-3 border-2 border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-emerald-500 focus:border-transparent transition-colors"
                    />
                    <div class="absolute right-3 top-3 text-gray-400">
                        🔑
                    </div>
                </div>
            </div>

            <!-- Giriş Butonu -->
            <button
                type="submit"
                class="w-full bg-gradient-to-r from-emerald-600 to-teal-600 hover:from-emerald-700 hover:to-teal-700 text-white font-bold py-3 px-4 rounded-lg transition-all transform hover:scale-105 active:scale-95 shadow-md"
            >
                ✅ Giriş Yap
            </button>

        
        </form>

        <!-- Alt Bilgi -->
        <div class="text-center mt-8">
            <p class="text-gray-600 text-sm">
                ← <a href="/" class="text-emerald-600 hover:text-emerald-700 font-semibold">Ana Sayfaya Dön</a>
            </p>
        </div>
    </div>
</div>

<style>
    /* Input focus animasyonu */
    input[type="password"]:focus {
        box-shadow: 0 0 20px rgba(16, 185, 129, 0.3);
    }
</style>
